<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Runtime-requested via firebase_messaging's requestPermission() call;
         Android 13+ requires the manifest declaration too. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Every network call (REST API, WebSocket, FCM) needs this. The debug/
         profile manifests inherit it from the Flutter template; declared
         explicitly here rather than relying on it being merged in from a
         dependency's own manifest (e.g. firebase-messaging). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="scenario_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!--
          allowBackup="false" because the app's private storage contains a live
          credential, not just preferences.

          The session cookie is persisted by PersistCookieJar as ordinary files
          under the app's documents directory. With backup enabled — the
          platform default — those files are copied into the user's Google
          Drive backup and into the device-to-device transfer that runs when
          they set up a new phone. That carries an authenticated Scenario
          session off the device it was issued to, where it can be restored
          onto another one; on API levels that still honour `adb backup`, it is
          extractable over USB without root.

          The two rules files above additionally exclude the cookie directory
          and the secure-storage preferences, so the exclusions survive anyone
          re-enabling backup later for a legitimate reason.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
