# تقرير فحص نظام الإشعارات (Push Notifications)

**ملاحظة حول النطاق:** تمت إعادة قراءة كل ملف في تطبيق Flutter من الكود الفعلي على القرص (وليس من الذاكرة)، وتم تأكيد نقطتين تغيّرتا منذ آخر مرة تم فيها التعامل مع هذا الجزء: **ملف `android/app/google-services.json` أصبح موجودًا الآن** (مشروع Firebase حقيقي باسم `scenario-d77de`، وحزمة التطبيق `com.scenario.scenario_mobile`)، بينما **ملف `ios/Runner/GoogleService-Info.plist` غير موجود**. أما مجلد الباك-إند (`SocialOmniChannelBackend/`) **فغير موجود في مساحة العمل هذه** — حالة git تُظهره كمحذوف، والأمر `ls` يؤكد ذلك. كل ما يلي إمّا (أ) تم تتبّعه من كود Flutter الفعلي مع الإشارة إلى الملف ورقم السطر، أو (ب) تم تحديده صراحةً بأنه "حسب التوثيق، غير مُتحقَّق منه" لأي شيء يخص الباك-إند، التزامًا بمبدأ "لا تخمين".

---

## 1. المعمارية الحالية (كما هي مُنفَّذة فعليًا)

```
Backend (غير مُتحقَّق منه — انظر القسم 4 و7 لمعرفة ما هو مطلوب للتأكيد)
   ↓
message.created (حدث WebSocket، في apps/realtime/events.py حسب التوثيق)
   ├── WebSocket → RealtimeClient.events → realtime_bridge.dart:_apply()
   │        → inboxControllerProvider.refreshQuietly()
   │        → إبطال conversationCountsProvider
   │        → إن كانت المحادثة مفتوحة: ConversationController.refreshFromServer()
   │        → ConversationController.markReadAndSync() عند وجود رسائل غير مقروءة
   │
   └── إشعار FCM (send_new_message_push، حسب التوثيق — غير مُتحقَّق منه في الكود المصدري)
            ↓
       Flutter: PushService (push_service.dart) — غلاف (wrapper) لـ Firebase
            ↓
       PushBridge (push_bridge.dart) — يوجّه حسب حالة التطبيق:
            • في المقدمة (foreground) → onForegroundMessage → _handleForegroundPush()
                            (نفس عملية إعادة الجلب التي تقوم بها جسر WS، بدون أي شارة/بانر مرئي)
            • في الخلفية (background) → يعرض النظام إشعارًا تلقائيًا
                            (الحمولة تحتوي على كتلة `notification`) →
                            الضغط عليه → onMessageOpenedApp → _handleNotificationTap()
            • عند إغلاق التطبيق (terminated) → يعرض النظام إشعارًا →
                            الضغط عليه → بدء تشغيل بارد (cold start) → getInitialMessage() →
                            _handleNotificationTap()
            ↓
       _handleNotificationTap() → router.go(Routes.conversation(id))
            ↓
       ConversationScreen.initState() → refreshOnOpen() → markReadAndSync()
```

حقيقة بنيوية مهمة، بما أنك طلبت عدم الافتراض: **حدث WS والإشعار عبر FCM موثّقان كمسارين متوازيين (siblings)، وليس أن أحدهما يسبّب الآخر.** حسب `NOTIFICATIONS_INTEGRATION.md` القسم 1، فإن الدالة `ingest_inbound_message()` تُضيف كلاً من `realtime.publish_conversation_event()` و`run_task(_send_new_message_push_safely)` إلى قائمة انتظار واحدة ضمن نفس كتلة `transaction.on_commit()`. اسم حدث WS، وهو `message.created`، هو مجرد تسمية لقناة البث اللحظي، وهو لا يُطلق الإشعار بذاته. لم أتحقق من هذا في الكود المصدري — انظر القسم 4.

---

## 2. ما يعمل فعليًا (مؤكَّد من الكود، مع رقم الملف والسطر)

| العنصر | الحالة | الدليل |
|---|---|---|
| تهيئة Firebase | ✅ | `push_service.dart:57-79` — استدعاء `Firebase.initializeApp()`، والفشل يُمتَص بدون تعطّل التطبيق |
| اعتمادية `firebase_messaging` | ✅ | `pubspec.yaml:48-49` (`firebase_core ^4.13.0`، `firebase_messaging ^16.5.0`) |
| إعدادات Firebase على أندرويد | ✅ **موجودة الآن** | `android/app/google-services.json` موجود، ويطابق `com.scenario.scenario_mobile`؛ `build.gradle.kts:10-12` يُفعّل الإضافة (plugin) بشرط، وسيتم تفعيلها فعليًا الآن |
| توليد رمز FCM (token) | ✅ | `push_service.dart:98-108` عبر `getToken()` |
| تسجيل الرمز لدى الباك-إند | ✅ | `push_bridge.dart:86-99` يستدعي `_registerDevice()` عند الإطلاق (إن كان المستخدم مسجّل الدخول) ومجددًا بمجرد الحصول على رمز |
| معالجة تحديث الرمز | ✅ | `push_bridge.dart:104-111` عبر `onTokenRefresh` → إعادة التسجيل، مع التحقق من حالة المصادقة |
| `onMessage` (المقدمة) | ✅ | `push_bridge.dart:112,166-205` |
| `onBackgroundMessage` | ✅ مُسجَّل، لكن انظر ملاحظة في القسم 3 | `push_service.dart:27-28,66-68` — دالة على المستوى الأعلى (top-level)، بعلامة `@pragma('vm:entry-point')`، بجسم فارغ (وهذا صحيح: الحمولة تحتوي دائمًا على كتلة `notification`، والنظام يعرضها دون الحاجة لتشغيل كود التطبيق) |
| `onMessageOpenedApp` | ✅ | `push_bridge.dart:113,210-228` |
| الضغط على الإشعار بعد إغلاق التطبيق كليًا | ✅، بما في ذلك حالة حافة (edge case) غير بديهية تمت معالجتها بشكل صحيح | `push_bridge.dart:118-126` عبر `getInitialMessage()`؛ تم تتبّع مسار "تسجيل الخروج عند بدء التشغيل البارد ← الضغط على الإشعار ← تسجيل الدخول ← لا يزال يوجّه للمحادثة الصحيحة" عبر آلية التحكم في `ScenarioApp` بملف `main.dart` (`auth.isRestoring`/`restoreFailedOffline`)، وتم التأكد أن `_initialMessageChecked` يؤجّل التحقق بشكل صحيح حتى أول تشغيل مصادَق عليه لـ `_setUpForSignedInAgent()` |
| طلب إذن الإشعارات | ✅ | `push_service.dart:83-96` عبر `requestPermission()` |
| الانتقال المباشر (deep-link) للمحادثة الصحيحة | ✅ | `push_bridge.dart:216-227` → `Routes.conversation(id)`؛ يعمل حتى مع انتهاء صلاحية الجلسة، لأن حارس المصادقة (auth guard) في الموجّه (router) يحافظ على `next=` (`app/router.dart:85-90`) |
| الحماية من الإشعارات المكرَّرة | ✅ بنيويًا، وليس عبر كود منع تكرار صريح | عقد Firebase نفسه يجعل `onMessage`/`onMessageOpenedApp`/`getInitialMessage` متبادلة الإقصاء (mutually exclusive) لكل تسليم؛ وإذا حدث أن WS وFCM كلاهما استدعيا `refreshFromServer()` لنفس المحادثة، فإن `ConversationController._isRefreshing` (`conversation_controller.dart:410-412`) يُسقِط الاستدعاء الزائد بصمت — لا جلب مزدوج، ولا خلل في الواجهة |
| تنظيف الحالة عند تسجيل الخروج | ✅ | `auth_controller.dart:92-98` يُلغي تسجيل الجهاز **قبل** حذف كوكي الجلسة |
| استرجاع الرموز المنتهية/غير الصالحة | ✅ (من طرف التطبيق) | `push_bridge.dart:153-160` — خطأ `not_registered` من نبضة التحقق (heartbeat) يُطلِق إعادة `register()` |
| إذن `POST_NOTIFICATIONS` على أندرويد | ✅ | `AndroidManifest.xml:4` |

السجلّ (log) الذي أرسلته (`WS_MESSAGE_RECEIVED` ← `DISPATCH` ← `BRIDGE` ← `ApiClient GET .../messages/` ← `STATE MESSAGE_ADDED` ← `UI MESSAGE_WIDGET_BUILT` ← `ApiClient POST .../read/`) يؤكد **مسار WebSocket فقط**. لا يحتوي على **أي سطر من `[PushService]` أو `[PushBridge]` أو `[DeviceRepository]`** — وقد أضفت هذه السجلات تحديدًا في هذا الكود لتكون هذه اللحظة بالذات مرئية. هذا الغياب دليل حقيقي، وليس تخمينًا، لكنه دليل على أمر محدود: *هذا المقتطف تحديدًا لا يُظهر أي نشاط متعلق بالإشعارات*. هذا لا يثبت بحد ذاته أن الباك-إند لم يرسل إشعارًا — انظر القسم 4.

---

## 3. ما هو ناقص / يستحق التحسين

لا شيء مما يلي "معطّل" — هذه فجوات مقارنةً بالقائمة الكاملة التي طلبتها:

1. **إعدادات Firebase لنظام iOS غير موجودة.** لا يوجد ملف `GoogleService-Info.plist` في `ios/Runner/`. سيفشل استدعاء `Firebase.initializeApp()` على iOS، وستبقى قيمة `PushService.isAvailable` مساوية لـ `false`، ولن يحصل iOS على أي رمز إطلاقًا. تم التأكد من ذلك عبر أمر `ls` — الملف غير موجود.
2. **لا توجد قناة إشعارات (notification channel) أو أيقونة مخصصة على أندرويد.** لا توجد بيانات وصفية (meta-data) باسم `com.google.firebase.messaging.default_notification_channel_id` أو `default_notification_icon` في `AndroidManifest.xml`. تعود FCM إلى قناتها الافتراضية (تظهر باسم "Miscellaneous" في إعدادات إشعارات أندرويد) وتستخدم أيقونة التطبيق الرئيسية كأيقونة في شريط الحالة، والتي يعرضها أندرويد كمربع/ظل أبيض بسيط عند استخدام أيقونات ملوّنة بالكامل. هذا أمر جمالي (cosmetic)، وليس وظيفيًا.
3. **لا توجد حزمة إشعارات محلية** (`flutter_local_notifications` غير موجودة كاعتمادية — تم التأكد من غيابها في `pubspec.yaml`). هذا قرار متعمَّد (حسب توثيق الباك-إند، فإن الـ socket هو قناة الإشعارات الأساسية أثناء وجود التطبيق في المقدمة)، لكنه يعني: **إشعار المقدمة (foreground) لا ينتج عنه أي شارة/بانر مرئي على الإطلاق**، حتى لو كانت المحادثة التي وصلت لها الرسالة غير التي يعرضها الموظف حاليًا. الإشارة المرئية الوحيدة هي ما يحدث عند إعادة رسم قائمة صندوق الوارد (شارة غير المقروء، إعادة ترتيب الصف) نتيجة التحديث المدفوع من WS. يستحق التأكد من أن هذا يطابق توقعات المنتج — فحاليًا شرط "تجنّب الإشعارات المرئية المكررة" مُحقَّق بشكل بديهي، لأنه لا يوجد أصلًا إشعار مقدمة يمكن أن يتكرر.
4. **تسجيل `Firebase.initializeApp()` و`onBackgroundMessage` مؤجَّل خلف حالة المصادقة**، وليس مستدعًى بشكل غير مشروط في أعلى `main()` (`push_bridge.dart:88`، لا يتم الوصول إليه إلا من `_setUpForSignedInAgent()`، والتي لا تعمل إلا عند تسجيل الدخول). توصية Firebase الرسمية هي تسجيل معالج الخلفية بشكل غير مشروط وفي وقت مبكر. عمليًا هذا غير ضار حاليًا — جسم المعالج فارغ، وكل حمولة تحمل كتلة `notification`، لذا يعرضها النظام دون تشغيل أي كود Dart بغض النظر عن حالة العملية (process) — لكنه انحراف يستحق الإغلاق كطبقة حماية إضافية (defense-in-depth)، مثلاً إن أصبحت حمولة مستقبلية تعتمد على البيانات فقط (data-only).
5. **لا يُرسَل طراز الجهاز أو إصدار التطبيق عند التسجيل** (فقط `push_token`، `device_identifier`، `platform` — `push_bridge.dart:132-136`). قرار متعمَّد (تجنبًا لإضافة اعتمادية `package_info_plus` لحقول اختيارية)، لكن أُشير إليه بما أنك طلبت التحقق من اكتمال حمولة التسجيل.

---

## 4. السبب الجذري — إن كانت الإشعارات لا تصل، إليك شجرة التشخيص المتفرّعة (وليس تخمينًا واحدًا)

لا يمكنني إعطاءك سببًا جذريًا واحدًا لأنني **لا أملك الكود المصدري للباك-إند ولا السجلّ الكامل من طرفك**، والتخمين هنا سيكون مخالفًا تمامًا لما طلبته. إليك شجرة القرار والدليل الدقيق الذي يحتاجه كل فرع:

**الفرع أ — تطبيق Flutter لم يُسجّل جهازًا/رمزًا أصلًا.**
تحقّق: هل يحتوي سجلّك الكامل (وليس المقتطف فقط) على `[PushBridge] _setUpForSignedInAgent() start` و`[DeviceRepository] register() succeeded`؟ إن ظهر `register() failed`، فتفاصيل `ApiException` (المُسجَّلة مسبقًا) تخبرك بالسبب. إن لم يظهر أي من السطرين، فهذا يعني أن `PushBridge` لم يشغّل `_setUpForSignedInAgent()` إطلاقًا — تحقّق مما إذا كان الجهاز مسجّل الدخول في تلك اللحظة (راجع تتبّع "حالة إغلاق التطبيق كليًا" في القسم 2).

**الفرع ب — تم التسجيل، لكن الباك-إند لا يُرسل الإشعار لهذا الحدث أبدًا.**
هذا هو "السؤال المهم" الذي طرحته. حسب التوثيق، فإن `message.created` (عبر WS) واستدعاء الإشعار مساران متوازيان (siblings) يُطلَقان من نفس كتلة `on_commit()` في `apps/conversations/services.py` — لكنني لم أطّلع على هذا الملف. من الممكن تمامًا أن يعمل مسار WS بينما لم يُربَط استدعاء الإشعار إطلاقًا، أو أنه مشروط بحالة معيّنة (محادثة غير مُسنَدة (unassigned) → التوثيق ينص صراحةً على *عدم إرسال إشعار حتى يتم التخصيص التلقائي*). **أحتاج إلى `apps/conversations/services.py` و`apps/realtime/push.py` للتأكد من هذا في أي اتجاه.**

**الفرع ج — تم الإرسال، لكن للمستلم الخاطئ أو مستبعَد بحسب التصميم.**
حسب التوثيق: الإشعارات تذهب فقط للموظف المُسنَدة له المحادثة، على أجهزته النشطة، ولا يتلقى الموظف أبدًا إشعارًا عن ردّه الصادر (لا يوجد إشعار ذاتي). لذا إن كنت تختبر بإرسال رسالة من نفس الحساب الذي "يستقبل"، فقد يكون غياب الإشعار **سلوكًا صحيحًا**، وليس خللًا. يجب التأكد: هل الرسالة الاختبارية قادمة فعليًا من عميل/قناة مختلفة تمامًا، إلى حساب غير المُرسِل؟

**الفرع د — الباك-إند يرسله، وFCM يسلّمه، لكن `SCENARIO_PUSH_ENABLED`/`SCENARIO_FIREBASE_CREDENTIALS` غير مُفعَّلَين في بيئة الاختبار.** حسب التوثيق، هذا يُسكِت الإرسال بصمت من جهة الخادم — بدون أي خطأ في أي مكان. **أحتاج لمعرفة إعدادات بيئة الباك-إند، أو سطر سجلّ يوثّق محاولة إرسال من Firebase Admin SDK، لاستبعاد/تأكيد هذا الاحتمال.**

**الفرع هـ — كل ما سبق صحيح، لكن `google-services.json` أُضيف *بعد* آخر عملية بناء/تثبيت لديك.** بما أنه موجود الآن على القرص لكن Gradle لا يلتقطه إلا في البناء التالي، فإذا كانت نسخة APK على جهاز الاختبار سابقة لإضافة هذا الملف، فإن Firebase يبقى غير متاح بصمت على ذلك التثبيت (يسجّل `ensureInitialized()` سطر `Firebase unavailable` — تحقّق من وجود هذا السطر). **قم بعملية بناء كاملة (`flutter clean && flutter run`) وتحقّق من ظهور `[PushService] ensureInitialized() succeeded` قبل أي اختبار إضافي.**

---

## 5. التغييرات المطلوبة (الحد الأدنى، بدون إعادة كتابة المعمارية)

فقط إن أكّدت الفرع المقابل أعلاه:

- **حل الفرع أ:** لا حاجة لأي تغيير في طرف Flutter إن أظهرت سجلات التسجيل نجاحًا؛ وإن أظهرت فشلًا، فتفاصيل `ApiException` المُسجَّلة مسبقًا تخبرك بالحل الدقيق (مشكلة CSRF/مصادقة/تحقق صحة على `/api/devices/register/`).
- **حل الفرع ب/ج/د:** يخص الباك-إند فقط، ولا يمكن حله من طرف Flutter. يحتاج إلى الملفات المذكورة في القسم 7.
- **حل الفرع هـ:** نفّذ `flutter clean && flutter run` على أندرويد لالتقاط `google-services.json`؛ أضف `ios/Runner/GoogleService-Info.plist` لنظام iOS (القسم 3.1).
- **القسم 3.2 (قناة/أيقونة الإشعار):** أضف إلى `<application>` في `AndroidManifest.xml`:
  ```xml
  <meta-data android:name="com.google.firebase.messaging.default_notification_icon"
             android:resource="@drawable/ic_notification"/>
  ```
  (يتطلب أصل أيقونة أبيض على خلفية شفافة — مهمة منفصلة صغيرة، وليست تغييرًا في منطق الإشعارات).
- **القسم 3.4 (تسجيل مبكّر لمعالج الخلفية):** انقل `Firebase.initializeApp()` و`FirebaseMessaging.onBackgroundMessage(...)` خارج `_setUpForSignedInAgent()` المشروطة بالمصادقة، إلى `main()`، بشكل غير مشروط، قبل `runApp()`. هذا نقل لعدة أسطر فقط، بدون تغيير معماري — `PushService.ensureInitialized()` تحمي أصلًا من التهيئة المزدوجة، لذا يكون آمنًا استدعاؤها هناك ومجددًا لاحقًا (بلا أثر إضافي).
- **القسم 3.3 (شارة/بانر المقدمة):** فقط إن كنت تريد فعليًا واحدة — يتطلب إضافة `flutter_local_notifications` ومعالج صغير داخل `_handleForegroundPush()`، مشروط بـ `active != conversationId` (بحيث لا تحصل المحادثة المفتوحة على شارة زائدة). لم أبنِ هذا لأنه لم يكن ضمن المواصفات الأصلية — أخبرني إن أردت هذا.

لم أطبّق أيًا من هذه التغييرات — حدّد أيها تريد وسأقوم بالتعديلات الدقيقة.

---

## 6. خطة التحقق (Verification / Test Plan)

| السيناريو | الخطوات | يؤكّد |
|---|---|---|
| **المقدمة (Foreground)** | التطبيق مفتوح، في صندوق الوارد (وليس المحادثة المستهدَفة). أرسل رسالة عميل لمحادثة مُسنَدة. راقب الطرفية (console) لظهور `[REALTIME] WS_MESSAGE_RECEIVED` **و** `[PushBridge] _handleForegroundPush()` معًا لنفس الرسالة. | أن كلا المسارين يعملان؛ يؤكّد أن الفرع ب ليس هو المشكلة إن ظهر سطر الإشعار |
| **المقدمة، مع فتح نفس المحادثة** | نفس ما سبق لكن مع فتح `ConversationScreen` لتلك المحادثة. تأكّد من عدم وجود ازدواجية/خلل في الواجهة، وتحديث واحد فقط. | عمل حارس `_isRefreshing` (القسم 2) |
| **الخلفية (Background)** | ضع التطبيق في الخلفية (دون إغلاقه). أرسل رسالة. تأكّد من ظهور إشعار نظام (هذا على مستوى النظام، مستقل عن كود التطبيق بمجرد تسجيل الرمز). اضغط عليه — تأكّد من ظهور سجلّ `[PushBridge] _handleNotificationTap()` والانتقال للمحادثة الصحيحة، مع تعليمها كمقروءة. | مسار `onMessageOpenedApp` |
| **إغلاق التطبيق كليًا (Terminated)** | أغلق التطبيق بالكامل. أرسل رسالة. تأكّد من ظهور إشعار النظام، اضغط عليه، وتأكّد من أن بدء التشغيل البارد يوصلك للمحادثة الصحيحة. | مسار `getInitialMessage()` |
| **الضغط على الإشعار مع جلسة منتهية** | ضع التطبيق في الخلفية، أنهِ صلاحية الجلسة يدويًا من الخادم، أرسل رسالة، اضغط على الإشعار. تأكّد من التوجيه لتسجيل الدخول، ثم الوصول للمحادثة بعد إعادة تسجيل الدخول. | الحفاظ على `next=` في الموجّه |
| **أجهزة متعددة** | سجّل نفس الحساب على جهازين. أرسل رسالة. تأكّد من استقبال كليهما للإشعار (ما لم يكن أحدهما بـ `notifications_enabled=false` أو `is_active=false` من جهة الخادم). | توزيع الإشعارات من جهة الباك-إند — يحتاج ملفات الباك-إند للتأكيد الكامل لمنطق الاستهداف |
| **التحقق من عدم الإشعار الذاتي** | أرسل ردًّا صادرًا كموظف مُسنَد للمحادثة. تأكّد من **عدم** وصول إشعار لنفس جهاز ذلك الموظف. | تأكيد ادّعاء التوثيق بـ"عدم الإشعار الذاتي" عمليًا |

لكل سيناريو، احفظ مخرجات الطرفية **الكاملة** (وليس مقتطفًا) بحيث تكون جميع أسطر `[PushService]`/`[PushBridge]`/`[DeviceRepository]`/`[ApiClient]` ظاهرة — هذا ما سيتيح لنا تحديد موقع الخلل بدقة ضمن شجرة القرار في القسم 4.

---

## 7. المعمارية النهائية (الهدف — تتطابق غالبًا بالفعل؛ التغييرات إضافية وليست بنيوية)

```
Backend
   ↓
إنشاء الرسالة
   ↓
message.created (مساران متوازيان، حسب التوثيق)
   ├── WebSocket → تحديث لحظي في Flutter (صحيح بالفعل، دون تغيير)
   │
   └── FCM → الجهاز
              ↓
       PushService.ensureInitialized()   [نقل إلى main()، بشكل غير مشروط]
              ↓
       PushBridge يوجّه حسب حالة التطبيق (صحيح بالفعل)
              ↓
       إشعار نظام (native) أو إعادة جلب صامتة (في المقدمة)
              ↓
       الضغط ← _handleNotificationTap() ← router.go(Routes.conversation(id))
              ↓
       ConversationScreen ← markReadAndSync()
```

جانب Flutter سليم بنيويًا ويطابق هذا الهدف بالفعل؛ الفجوات هي ملف إعدادات iOS، وتأجيل تسجيل معالج الخلفية، وإعدادات القناة/الأيقونة الجمالية على أندرويد — لا شيء من هذا يتطلب لمس `RealtimeClient` أو `RealtimeBridge` أو أي كود خاص بـ WebSocket.

---

## ما أحتاجه منك لإغلاق الفرع ب/ج/د (الباك-إند)

بما أن `SocialOmniChannelBackend/` غير موجود في مساحة العمل هذه، لا يمكنني التحقق من نصف هذا التقرير الخاص بالباك-إند إطلاقًا في الوقت الحالي. لإنهاء الفحص بدقة بدلًا من تركه معلّقًا كـ"حسب التوثيق"، سأحتاج إلى:

- `apps/conversations/services.py` (دالة `ingest_inbound_message` وكتلة `on_commit`)
- `apps/realtime/push.py` (دوال `send_new_message_push`، `send_assignment_push`، منطق استهداف المستلم، استبعاد المُرسِل)
- `apps/accounts/models.py` (نموذج `EmployeeDevice`، للتأكد من تطابق البنية مع ما يرسله `device_repository.dart`)
- الملف الذي يُعرِّف `run_task(...)` (يؤكّد ما إذا كان الإرسال متزامنًا أم عبر طابور/غير متزامن)
- تأكيد وجود `SCENARIO_PUSH_ENABLED` و`SCENARIO_FIREBASE_CREDENTIALS` في بيئة الاختبار التي تستخدمها

أو، بشكل أسرع: نفّذ اختبار "المقدمة (Foreground)" في القسم 6 وأرسل لي مخرجات الطرفية **الكاملة** — هذا وحده كفيل بإخبارنا ما إذا كانت المشكلة من طرف Flutter أو من طرف الباك-إند دون الحاجة للكود المصدري.
